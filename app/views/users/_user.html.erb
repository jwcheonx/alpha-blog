<tr>
  <td><%= user.username %></td>
  <td><%= user.email %></td>
  <td><%= time_ago_in_words(user.created_at) %> ago</td>
  <td><%= user.articles.size %></td>
  <td><%= link_to 'View', user %></td>
  <td><%= link_to_if logged_in? && user == current_user, 'Edit', edit_user_path(user) %></td>
</tr>

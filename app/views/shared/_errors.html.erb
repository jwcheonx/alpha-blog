<% if obj.errors.any? %>
  <h2>Could not save the <%= obj.class.name.downcase %> due to the following errors:</h2>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
